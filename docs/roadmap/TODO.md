# TODO / Module Sequence

Per the project's "module by module, verify before proceeding" rule — each item below is planned, built, and verified (architecture, scalability, database, APIs, UI) before the next starts.

- [x] **Foundation Scaffold** — folder structure, config/logging/security primitives, response envelope, Docker/CI, docs.
- [x] **Authentication — approved and frozen.** Owner/Employee mobile+password login; invitation-only Customer/Referral Partner signup (Owner/Employee-authorized, decision 011); forgot/reset/change password; JWT + refresh rotation with family/reuse detection (decision 013); login lockout; sessions with Login History fields; audit logs. 9 endpoints, 4 frontend pages. 20/20 tests passing (`tests/api/test_auth.py`). Full detail: `docs/AUTHENTICATION.md`. **No architectural changes to this module without explicit approval — future modules consume its APIs, they don't modify it.**
- [x] **User & Employee Management — approved and frozen.** Owner-managed Employee CRUD (search/filter/export/sessions/history/activity-summary), Reset Password/Force Logout (reusing Auth internals unmodified, decision 015), self-service Employee profile + photo, departments/designations/branches (seeded, temporary home per decision 019), employee documents (API only). No permission engine yet (Module 3). 24 endpoints, 7 frontend pages. 40/40 tests passing across the suite (`tests/api/test_employee.py` + `test_auth.py`). **No architectural changes to this module without explicit approval — future modules consume its APIs, they don't modify it.**
- [x] **Role, Permission & Access Control — approved and frozen.** Data-driven permission catalog (free-text `module`/`resource`, fixed `PermissionAction` vocabulary — decision 021); Role CRUD/activate/deactivate/duplicate; Permission Matrix (bulk get/set, optional department/branch scope); Employee↔Role assignment + accessible-modules lookup; Temporary Access and Geo Exceptions (daily recurring window, lazy expiry — decision 022; Geo Exception administrative-only — decision 023); `PermissionEngine`/`require_permission` for future modules to gate their own routes (Module 2 deliberately not retrofitted — decision 024); audit logging for all changes. 23 endpoints, 6 frontend pages. 54/54 tests passing across the suite (`tests/api/test_access_control.py` + prior). Full detail: `docs/PERMISSIONS.md`. **No architectural changes to this module without explicit approval — future modules consume `require_permission(...)`, they don't modify its engine, models, or APIs.**
- [x] **Settings (Master Data) — approved and frozen.** Departments/Designations/Branches edit/activate/deactivate (Module 2's collections, extended — decision 025); full CRUD for Lead Sources/Loan Products/Insurance Products/Document Types (one shared `NamedMasterData` code path — decision 027); Status Masters, Notification Templates (both seeded empty — real content is unconfirmed business logic, decision 029); API Settings (encrypted config blob, write-only — decision 028); Company Settings (singleton — decision 030). First real consumer of `require_permission` — every endpoint is gated by it, not a bespoke role check (decision 026). ~60 endpoints, 11 frontend pages. 65/65 tests passing across the suite (`tests/api/test_system_settings.py` + prior). **No architectural changes to this module without explicit approval — future modules consume its master-data collections (read) and `require_permission("system_settings", ...)`, they don't modify its models, engine, or APIs.**
- [x] **Dashboard Framework — approved and frozen.** Real `AppShell` (Sidebar/Topbar/`<Outlet/>`) wraps every authenticated route, replacing the Foundation placeholder (decision 035); DB-driven Sidebar nav gated to mirror each route's actual protection (decision 033); Topbar Quick Search/Notification Bell/Profile Menu (working Logout — decision 034); 13-widget Dashboard Engine, permission-based and forward-compatible with unbuilt modules (decision 032, verified end-to-end), configurable per user (hide/show/order/refresh interval — decision 036). 3 widgets real (Recent Activities, Department/Employee Summary), 10 honest placeholders (decision 038). No Lead/Customer CRUD or Loan/Insurance processing — framework only, as directed. 11 endpoints, 76/76 tests passing across the suite (`tests/api/test_dashboard.py` + prior). **No architectural changes to this module without explicit approval — future modules add their own nav item + widget wiring as part of their own build.**
- [ ] **Lead Management (Module 6)** — the largest module; per the user's explicit instruction, split into four sub-modules built and verified one at a time rather than in one pass:
  - [x] **6A — Lead Foundation — approved and frozen.** Lead entity/CRUD, Lead Sources (Module 4's `lead_sources`, reused), duplicate detection (flags via `duplicate_of_lead_ids`, never blocks — decision 041), assignment, merged Timeline (Activities+Notes, decision 042), search/filters/CSV export. Status fixed to "New" only — the real pipeline is 6C's job. Wired 2 of Dashboard's placeholder widgets (Today's Leads, Assigned Leads) to real data, verified end-to-end (decision 044). Gated by `require_permission("leads", "leads", ...)`. 10 endpoints, 3 frontend pages. 88/88 tests passing across the suite (`tests/api/test_leads.py` + prior). **No architectural changes to this sub-module without explicit approval — 6B builds on top of it, converting a Lead, rather than modifying its models, engine, or APIs.**
  - [x] **6B — Customer Onboarding & Application Flow — approved and frozen, reconfirmed after 3 business-rule refinements.** Both entry flows (secure link from a Lead; direct portal registration) reuse Module 1 (Auth) 100% unmodified via a resolved `inviter` (decisions 046/049), merging into one shared Application form/validate/submit path once the form opens. Customer Profile is a fixed schema, collected at registration (Flow 2) or inline pre-conversion (Flow 1, decision 047). Lead→Customer conversion is a reverse pointer (`Customer.converted_from_lead_id`) — Module 6A's `Lead` untouched (decision 048). Dynamic Application Form Engine: seeded `ApplicationFormDefinition` (product-specific fields only, **explicitly temporary development data, not frozen** — decision 051 amendment) + a generic renderer, required documents from Module 4's `document_types`. Owner/Employee staff visibility uses plain role checks, not `require_permission` — inherent, not delegable (decision 050). Post-approval refinements (decision 053): direct-portal registrations are no longer attributed to a seeded Owner and instead land in an Owner-only "Unassigned Applications" queue (`unassigned_only` filter); a "Generate Secure Link" button was added to Lead Details as an approved UX exception (decision 052 resolution). ~25 endpoints, 13 frontend pages. 96/96 tests passing across the suite (`tests/api/test_customer.py` + prior). **No architectural changes to this sub-module without explicit approval — 6C builds the real status engine on top of `Application`, it doesn't modify this sub-module's models, engine, or APIs.**
  - [x] **Module 6C Workflow Proposal — approved** (with clarifications resolved before coding): unified `application_workflows` collection, two rejection exit points per pipeline, no rollback in v1, real `require_permission` gating.
  - [x] **6C — Loan & Insurance Processing Pipeline — approved and frozen**, after one final business refinement. First real use of `workflow_engine`/`event_engine` (reserved since Foundation, decision 005): a generic, case-type-agnostic `WorkflowEngine` drives both pipelines off seeded `workflow_definitions` rows. One unified `application_workflows` collection (decision 054), not split loan/insurance collections — reverse-points at Module 6B's frozen `Application` (decision 048's pattern), case created lazily on first read rather than via a live hook into 6B's frozen `submit_application` (decision 058). Loan: New Customer → Documents Pending → Credit Evaluation → Offer Acceptance → Additional Documents → eSign/NACH/KYC → Final Evaluation → Rejected/Send for Disbursement → Disbursed, two rejection exits (decision 055). Insurance: its own **finalized** lifecycle (decision 064, superseding the draft flagged as an assumption in decision 057): Application Submitted → Documents Pending → Underwriting → Medical Verification (optional) → Additional Documents (optional) → Premium Acceptance → Policy Generation → Policy Issued. Bank/NBFC recorded as case fields, not a stage (decision 061). Real `require_permission("loan_management"|"insurance_management", "applications", ...)` gating, reversing 6B's decision 050 (decision 059). "On Hold"/"Resume" added as an Optional Status on both pipelines' workflow definitions, not hardcoded (decision 064) — no rollback otherwise (decision 056). No Customer-facing frontend built, per the explicit "Do NOT build Customer Portal enhancements" instruction (decision 062) — the Customer self-service API exists, untested by any UI screen. Dashboard's Disbursed/Rejected widgets wired to real data (decision 063). ~30 endpoints, 4 staff frontend pages. 105/105 tests passing across the suite (`tests/api/test_workflow.py` + prior). **No architectural changes to this module without explicit approval — Module 6D consumes its data (e.g. a Rejected case feeding the 90-day reminder) rather than modifying its engine, models, or APIs.**
  - [x] **6D — Reminder & Notification Engine — approved and frozen.** Internal database notifications only — no WhatsApp/SMS/Email/push/external API (decision explicit instruction; `notification_management` stays reserved). New `Task` entity (Owner assigns, Employee completes) — first real use of Dashboard's pre-reserved `reminders`/`tasks` widget gate (decision 068). `ReminderRule` — one collection discriminated by `rule_type`, Owner-editable, timings never hardcoded (decision 067): seeded Loan/Insurance 90-day re-eligibility (10-day notice) and a 30-minute task-due reminder with a 60-minute/2-repeat escalation ladder to the assignee, then one final notification to every Owner (decision 069). Internal Notification Queue + History (unread/read/archived/dismissed, self-service — decision 066/071). First real Arq jobs (decision 002): `poll_audit_events` (every 5 min, reacts to `lead_assigned`/`document_uploaded` by polling `audit_logs` rather than a live hook into those frozen modules — decision 058 → 065), `check_task_reminders` (every 15 min), `check_re_eligible_cases` (daily). Dashboard's Tasks/Notifications widgets wired to real data (decision 070). Fixed a latent, project-wide datetime-comparison bug along the way (`ensure_utc`, decision 072). ~16 endpoints, 3 frontend pages. 112/112 tests passing across the suite (`tests/api/test_reminders.py` + prior). **No architectural changes to this module without explicit approval — future modules consume its `Notification`/`Task` model rather than modifying its engine, models, or APIs.**
- [x] **Referral Partner Portal (Module 7) — approved and frozen.** Built from a written design proposal reviewed and refined before implementation (5 refinements folded in — decisions 075–081). Owner-created partner + Auth's own unmodified invite mechanism (decision 076), with a second, independent `approval_status` gate separating "can log in" from "can act." "Add Lead"/edit reuse Module 6A's unmodified `LeadService` — a new `referral_leads` mapping collection, `Lead` untouched (decision 077). External status is a fixed four-value vocabulary (`submitted`/`in_progress`/`approved`/`rejected`), never an internal status; the edit cutoff is "processing started" (Employee assigned or Application exists), not `Lead.status` (decision 078). `CommissionRule` (Owner-configurable, product+partner scoped, never hardcoded) and `CommissionEntry` (full snapshot at trigger time — a later rule edit never changes a historical payout, decision 079), created by a new daily `check_commission_triggers` Arq job. Manual settlement only, no payment gateway. Referral Partner hierarchy reserved, not implemented (decision 080). Every management endpoint `require_owner`-gated, no Employee capability (decision 081). ~19 endpoints, Owner pages (inside the existing Dashboard shell) + a new, separate Referral Partner Portal shell. 117/117 tests passing across the suite (`tests/api/test_referral_partner_management.py` + prior). (The Customer Portal, Loan Management, and Insurance Management items previously listed separately here are now covered by 6B and 6C above — removed as duplicates, not dropped.)
- [ ] **Customer Portal enhancement: Loan/Insurance case status + offer/premium accept-decline UI** — explicitly excluded from Module 6C's scope (decision 062); the backend API (`GET /loan-cases/mine`, `POST .../offer/accept`/`decline`, and the Insurance equivalents) exists and is tested, but no Customer-facing screen reaches it yet.
- [x] **Reports & Analytics (Module 8) — approved and frozen.** Per explicit instruction, the **Reporting Framework** was built first: a Report Engine (decision 082), an Aggregation Layer (4 generic Mongo helpers, decision 083), a shared Date Range convention, CSV Export, Saved Filters (self-service, decision 086), and Scheduled Reports (CRUD/storage only, no execution — explicitly framework-only, decision 085). Per a follow-up review, Report Definitions became **data, not code** — a seeded `report_definitions` collection (decision 090) drives 10 of 17 reports as pure configuration via a generic executor (`count`/`sum`/`list`); the remaining 7 with genuinely custom logic still need a function (`CUSTOM_REPORT_RUNNERS`), but their catalog metadata is data too. 17 Business Reports: Lead (by Source/Conversion/by Employee/by Product), Loan (Applications/Approved/Rejected/Disbursed/Bank Performance), Insurance (Applications/Policies Issued/Rejections), Employee (Task Summary), Referral (Leads/Conversions/Commission), and Dashboard Analytics (recomputes Dashboard's own metric definitions read-only rather than importing its frozen `widget_providers.py`, decision 084). Delegable via `require_permission("reporting", ...)`, unlike Module 7's Owner-only posture (decision 087). A handful of reports' date/status approximations (e.g. no dedicated `rejected_at`) are recorded as a deferred Phase 2 schema improvement to `ApplicationWorkflow`, not fixed now since Module 6C is frozen (decision 089). ~13 endpoints, 3 frontend pages (a fully generic report viewer, not one page per report). 127/127 tests passing across the suite (`tests/api/test_reporting.py` + prior).
- [ ] **External Integrations (Module 9)** — split into 4 sub-modules, each depending on the previous, per explicit instruction; no sub-module starts until the previous one is explicitly approved:
  - [x] **9A — API Management — approved and frozen.** A reserved (not implemented) `health_status` field and confirmation that `integration_test_logs` is permanent, unbounded history were folded in before freeze (decision 096). Configuration/management platform only (no message sent, no lead fetched, no business action beyond an optional Test Connection). New, additive `features/integrations` — not an extension of frozen `system_settings.ApiSetting` (decision 091). Multiple named configurations per integration type, exactly one Active (decision 092, the user's own recommendation, given before implementation). One flexible encrypted config blob per config (reusing Foundation's `security/encryption.py`, decision 093) — secrets masked to their last 4 characters in every response, detected by naming convention. Test Connection: real live checks for Meta/Google Maps (well-known APIs), generic reachability for WhatsApp/SMS/API-based Email (open-ended, multi-provider by design), a real SMTP handshake with no mail sent (decision 094). Seeded `integration_providers` catalog — "Integration Type → Provider → Configuration → Status," never hardcoded (decision 095). ~11 endpoints, 2 frontend pages (Integration List with an Add Configuration form; Integration Details with Enable/Disable/Activate/Test Connection/edit/test history) — no webhook setup screens, per explicit scope. 134/134 tests passing across the suite (`tests/api/test_integrations.py` + prior).
  - [x] **9B — Lead Capture — approved and frozen.** One shared Webhook → Provider → Parser → Lead pipeline (decision 097) — Website form, Meta Lead Ads (real HMAC-SHA256 signature verification + live Graph API lead retrieval, decision 100), and Manual API all reuse Module 6A's frozen `LeadService.create_lead` directly. A system-actor pattern for unauthenticated captures, `created_by` nulled out afterward (decision 098, same precedent as 6B's own `_any_owner()`); real provenance recorded on the Lead's own Timeline instead. `capture_failures` (duplicate/invalid_data/missing_required_fields/api_error — never silently dropped, decision 099) with a 15-minute Arq retry queue (only `api_error` is retried, exponential backoff, gives up after 5 attempts). Idempotency via `capture_receipts`, only where a real external id exists — Meta (decision 101). Seeded, Owner-remappable Source Mapping (decision 102). A reserved `webhook_events` collection and a Lead Source Metadata / Website Form Version passthrough (campaign/ad set/ad/UTM, captured only when a source's payload supplies it) were folded in at freeze review (decision 103). ~8 endpoints, 1 frontend page. 144/144 tests passing across the suite (`tests/api/test_lead_capture.py` + prior).
  - [x] **9C — Communication Engine — approved and frozen.** `Business Module -> Communication Service -> Queue -> Provider Adapter -> Provider -> Delivery Status -> Communication History` (decision 104) — no frozen module is called directly; a worker poller consumes 5 named business events (Lead Assigned, Reminder Triggered, Application Submitted, Document Requested, Commission Ready) from the existing `audit_logs` collection, the same pattern Module 6D established (decision 065). A common Provider Adapter interface with real WhatsApp/SMS sends (generic HTTP POST) and a real Email send (SMTP via `smtplib`, or a generic HTTP POST for an API-based provider) (decision 105). Owner-authored templates only, `{{variable}}` substitution, ships with zero seeded rows (same posture as Notification Templates, decision 029); OTP is a reserved category only — Auth is frozen and OTP must stay synchronous, so it's never queued (decision 106). Read-only recipient resolution against each frozen module's own repository (Employee/Customer/ReferralPartner/User), a dedicated `communication_checkpoints` polling cursor (decision 107). Retry only transient failures, exponential backoff, `MAX_RETRY_ATTEMPTS=5`; permanent failures go straight to `failed`; the manual Retry Action reuses the same send path (decision 108). ~10 endpoints, 1 frontend page (Templates / Queue & Failed Messages / Delivery History tabs) — no campaign builder, no bulk messaging. 151/151 tests passing across the suite (`tests/api/test_communication.py` + prior). One unrelated pre-existing frontend bug in the frozen `LeadCapturePage.tsx` was fixed as part of this round's verification (a `useEffect` callback returning a `Promise`).
  - [ ] **9D — Future Integrations** *(next)* — Razorpay, Bank APIs, eSign, Credit Bureau, other providers.
- [ ] **Mobile APIs** — Confirm/adjust API contract against first Flutter screen.
- [ ] **Testing** — Fill in `tests/unit`, `tests/integration`, `tests/api`, `tests/e2e`.
- [ ] **Deployment** — AWS/GCP target validation.
- [ ] **Production Release**

**Deferred Phase 2 schema improvement** (decision 089, recorded during Module 8's review, not implemented — Module 6C is frozen): add explicit `submitted_at`/`approved_at`/`rejected_at`/`disbursed_at`/`policy_issued_at`-style timestamps to `ApplicationWorkflow`/`LoanCaseDetails`/`InsuranceCaseDetails`, replacing Reporting's current `updated_at`/`offer_decision` approximations (decision 088) with real, purpose-built fields. Implement only if reporting accuracy requirements demand it later — an approved business change to a frozen module, not a default next step.

Business-logic questions that must be resolved *before* their owning module starts (not invented):
- Geo-fencing's concrete use case (employee attendance? login location restriction? something else).
- Concrete Status Master values (Loan/Insurance/Customer status pipelines) and Notification Template copy — the Settings module (Module 4) ships the collections and full CRUD UI for both, deliberately seeded empty (decision 029).

Resolved during Module 6C's pre-implementation clarification round and its subsequent business review (see `docs/decisions/DECISIONS.md` #054–#059, #064): which Lead/Case Status stages allow rejection (two exit points per pipeline, not only the final stage — decision 055); Loan vs. Insurance's status model is a unified collection with a `case_type` discriminator, not two separate collections (decision 054); the exact Insurance lifecycle stage names (decision 064, finalized — superseded the flagged assumption in decision 057); no rollback in v1, though a temporary "On Hold"/"Resume" pause exists as an Optional Status (decision 056/064). Resolved during Module 6D: the 90-day re-eligibility reminder keys off Rejected Loan/Insurance Cases (Module 6C), not `Lead` — Module 6A's Lead has no rejected status (decision 065's scan, timings from an Owner-editable `reminder_rules` row, decision 067). Resolved during Module 7's pre-implementation design proposal (decisions 075–081): Commission calculation rules for Referral Partners are Owner-configurable `CommissionRule` data (percentage or flat, scoped by product/partner, keyed to a trigger event), never hardcoded — and every payout is a frozen snapshot at trigger time, immune to later rule edits (decision 079).
